# Sandbox-Exec findings

| # | 发现 | 影响 | 建议 |
|---|---|---|---|
| 1 | **afterShellCleanup 的 /terminate 导致 shell 间歇性失败**（~86% 失败率）：每次 shell 后杀 box，下一次需重建，时序敏感 | S1/S2 轮次膨胀（30/27 vs 预期 ~7）；模型在成功/失败间循环 | **下一轮最优先修**：移除每-shell后的 /terminate，改回会话结束时统一清理（M3-C 原始设计），或加 500ms 防抖 |
| 2 | S1 的 3 次 edit 中有 2 次是非必要修改（改了 ValueError 消息和 print 格式，任务只要求修 people=0） | 过度修改，但最终结果正确 | 提示词"只做明确要求的改动"未完全生效；真实使用观察 |
| 3 | R1 本轮只用了 1 轮（读文件后直接回答"已全部是中文"）——前次测试的修改已 git reset，文件确实是英文的，模型读后回答可能不准确 | R1 数据可能无效 | 检查 reset 是否完全恢复到初始 commit（含英文消息的版本） |
| 4 | S2 的 4 次写中 2 次是 write（全文件覆盖）——编码 re-edit 循环仍在（T1 提示词修复部分缓解但未根治） | 与 /terminate 问题叠加 | /terminate 修复后重测，可能自然消失 |
