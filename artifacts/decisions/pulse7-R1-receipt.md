# pulse7 — F1–F4 交付复核回执（R-1）  > 收到 `f1f4-report.md`。日期：2026-09-09。 > **结论：披露纪律达标，功能修复方向正确，但不判"完成"。** 有一个阻断项、三个必须回答的问题、一项工作树收口。 > 本回执的编号项全部回答完毕之前，**不要开始阶段三，不要动 F1–F4 之外的任何代码。**  ---  ## 0. 先做：工作树与分支收口（**在跑任何东西之前完成**）  现在源码分散在多个位置和分支上，交付物、评审制品、历史检出混在一起，已经出现"这份证据是在哪棵树上跑的"说不清的风险。**先把这件事收干净，再谈验证。**  ### 0.1 报一份现状清单  在**开始任何测试之前**，输出到 `artifacts/worktree-inventory.md`：  - `git worktree list` 全量输出； - `git branch -vv --all` 全量输出； - 对每一棵工作树：路径、当前分支、HEAD 短哈希、`git status --porcelain` 是否干净（脏则列出文件）； - 每棵树各自的用途一句话（开发 / 只读历史 / 评审制品 / 已废弃）； - **本轮报告里每一份证据文件，分别是在哪一棵树、哪个 commit 上产生的。**  ### 0.2 唯一开发树  - \*\*唯一可修改的工作树：`E:\codex-worktrees\win7-agent\pre-phase3`，分支 `pre-phase3`。\*\* - `E:\win7-agent` \*\*只读\*\*，仅用于读取历史文档与 `tools\go`。\*\*任何情况下不得在其上创建分支、提交、打 tag、修改文件。\*\* 若报告中任何证据是在它上面产生的，明确指出是哪几份。 - 后续所有 R2、S3、基线对比运行，\*\*全部在唯一开发树或其明确标注的临时克隆中进行\*\*，产物统一落到 `<开发树>\artifacts\`。 - 基线对比（`3303809`）需要另一棵树时：\*\*用 `git worktree add` 建临时树，命名 `baseline-3303809`，用完 `git worktree remove`\*\*，并在报告中记录建立与移除。不要在别处随手 clone，不要复用旧目录。  ### 0.3 分支收口  - \*\*不得新建分支。\*\* 本轮所有工作留在 `pre-phase3` 上。 - 若已存在本轮过程中创建的临时分支/克隆，在清单里列出，说明它是什么、还需不需要；不需要的\*\*先报告再删\*\*，不要自行清理。 - tag 规则不变：`rc-0.11` 指向本快照，已有 tag 一律不动。  \*\*0.1 的清单交上来之前，不要开始下面任何一项。\*\*  ---  ## 1. 阻断项：R2 必须跑（\*\*本轮最重要的一项\*\*）  F1 的全部立论是"它解释了 R2 长期不收敛"。R2 没跑，F1 就只有单元测试证据，\*\*缺少它声称要解决的那个现象本身的证据\*\*。  ### 1.1 任务定义：确认沿用历史用例  你定位到的 `poc/scripts/ff-e2e.cmd` 中的任务文本——  > 说明这个项目的入口文件和主要模块分工。不要修改任何文件。  ——\*\*确认沿用这一条\*\*，形态正确（只读探索）。把它连同目标工作区路径固化成 `artifacts/r2-task.txt` 并提交，后续复现直接引用该文件。  若该脚本中同时记录了历史 R2 的目标工作区，\*\*沿用同一个目标工作区\*\*；若没有记录，选定一个后在报告中写明，并注明"目标工作区与历史运行是否同一，无法确认"。  ### 1.2 三次运行（\*\*两次验收 + 一次对照\*\*）  | # | 配置 | 性质 | |---|---|---| | 1 | 产品默认预算，不传 `--max-ctx` / `--max-rounds` | \*\*验收\*\* | | 2 | 同上，独立重跑 | \*\*验收\*\* | | 3 | `--max-ctx 12000` | \*\*对照，非验收项\*\*，报告中必须显式标注 |  \*\*第三次是本轮的关键，不要省。\*\* 理由：  报告称产品默认为 48000 字节（≈12000 估算 token），而历史 R2 用的是 `--max-ctx 12000`。\*\*先回答一个前置问题：`--max-ctx` 的单位是字节还是 token？\*\*  - 若与默认同为\*\*字节\*\* → 历史 R2 跑的是默认预算的 **1/4，截断因此频繁触发； - 那么在默认预算下，截断\*\*可能根本不触发\*\*——此时 R2 收敛了也证明不了 F1\*\*，因为 F1 修的是截断行为，得在会截断的配置下才看得见。  所以第三次运行才是 F1 的直接证据：\*\*同一个窄配置上，旧代码把请求从 15273 字节砍到 91 字节，新代码能保住最新工具结果并收敛。\*\*  任务书要求的"每次截断保留/丢弃了哪些组、丢弃多少字节"这项数据，也只有在会触发截断的运行里才拿得到。  ### 1.3 每次运行必须记录  轮次 / tree 调用次数 / ls 调用次数 / read 调用次数 / 截断次数 / 压缩次数 / 是否收敛 / 工作区是否零改动 / \*\*每次截断保留了哪些组、丢弃了哪些组、丢弃多少字节\*\*。  三次运行分别附持久化会话文件。\*\*无论结果如何都如实记录；仍不收敛就写仍不收敛，不得调提示词、不得调轮次上限、不得挑一次好的报上来。\*\*  ---  ## 2. 必须回答的三个问题  ### 2.1 S3 的 PARTIAL 是不是退化？  报告只说"给出多个候选问题而非恰好一个"，没有给出修改前的基线行为。全局验收 #7 是"S1/S2/S3/R1 无退化"，不知道基线就无法判定。  \*\*要做**：在 `3303809`（临时基线树，见 §0.2）上跑一次 S3，同条件对比，附两份日志。  \*\*要注意\*\*：如果基线也是多候选问题，那说明验收标准本身过严，\*\*该改的是判据措辞，不是代码\*\*。  \*\*无论如何都不要为了凑"恰好一个问题"去改提示词或加约束逻辑。\*\* 那是让代码替模型思考，属已否决方向（§1.1 / §0.5 第 1 条）。  ### 2.2 `dirty-files=2524` 是不是本轮引入的？（\*\*三项里最要紧\*\*）  R1 中 checkpoint 显示 `dirty-files=2524`，而普通 git status 执行前干净、执行后只有指定文件变化。  \*\*这是系统在报告一个它并不掌握的事实\*\*（§0.5 第 6 条），而且发生在 rollback 安全网这条路径上。时间点也可疑：它与本轮"工作区边界检查从文件操作搬到 checkpoint 校验"在同一次交付里。  \*\*要做\*\*：在基线 `3303809` 上复现同一场景，确认该计数器在改动前是什么值，附两侧证据。  - \*\*若基线也是 2524\*\* → 属既有问题，记入 findings，本轮不修； - \*\*若基线正常、本轮才出现\*\* → 属 F4 的连带影响，\*\*必须本轮查清\*\*。  理由：checkpoint 覆盖面算错，rollback 就不可信；而 F4 敢放行工作区外写，前提正是"工作区内的改动有 checkpoint 兜底"。\*\*这个前提如果被本轮自己动坏了，F4 就不成立。\*\*  ### 2.3 边界检查搬家算不算结构性改动？  任务书写明"若需对 `pathpolicy_windows.go` 做结构性改动则停止并报告"，本轮做了"工作区边界检查从每个文件操作移到 checkpoint 校验"而未停。  搬家本身\*\*可能是合规的\*\*——例外①要求 checkpoint 仍拒绝 junction 外逃，那个检查必须留在某处。但需要明确回答：  1. 除了批准清单中的 18 条断言，\*\*还有没有其他行为发生了变化\*\*？ 2. \*\*文件操作路径上现在还剩哪些校验\*\*？逐项列出（路径解析、`.git`、只读模式、文件新鲜度、hard link 多链接检测……），并指出各自现在在代码中的位置； 3. 有没有哪条校验因搬家而\*\*在某些路径上不再执行\*\*？  \*\*如实回答即可，不需要回滚改动。\*\* 目的是让下一轮接手的人知道边界现在在哪。  ---  ## 3. 记账（本轮不修，但要留档）  1. \*\*lifecycle 用例现在是两个\*\*：`TestJobObjectProcessCountIncludesSessionChildren`（三次复现一次）、`TestExitSummaryListsHarvestedAndDetachedTasks`（基线未复现，成因未证）。按原定单独处理，\*\*不并入功能修复\*\*。两者一并记入 findings，注明下一轮需要一个专门窗口。 2. \*\*本轮真实模型跑的是 `aigc789.top`，不是内网端点。\*\* 因此人工事项"内网速度实测"\*\*仍然挂着\*\*，这几次运行不能顶替它。请在报告中明确写一句，避免后续误当已完成。 3. 默认 30 vs 100 轮次上限的差异先于本轮存在，NOT MET 的判定正确，保持原样。  ---  ## 4. 本回执的交付要求  - §0.1 工作树清单\*\*先单独交一次\*\*，等确认后再往下做； - §1、§2 完成后，更新 `artifacts/f1f4-report.md`（追加"复核回执 R-1 应答"一节），不要新开报告文件； - 每一条结论仍按原标准：\*\*实测通过（附证据）/ 未验证\*\*，两种状态，没有第三种； - \*\*本回执范围之外的任何问题，一律记 findings，不要顺手修。\*\* 

