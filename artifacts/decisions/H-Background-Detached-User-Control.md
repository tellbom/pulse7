# 用户裁决：后台保留由用户决定（2026-09-12）

第三方后代脱离是 JOB_OBJECT_LIMIT_SILENT_BREAKAWAY_OK 的预期行为。若目标程序或其后代持续运行、持有输出管道，不因回收完整性强制终止；长期运行且需退出后保留时采用 background=true、detached=true，返回任务标识，把查看与关闭选择交给用户，遵循高自治、低摩擦方向。detached 单独使用仍属于前台等待语义，本次不重写该等待链路。

386 的既有时序波动暂时保留，历史失败与隔离复跑证据不变；暂停追加复测，后续 UI 阶段由用户实测观察能否复现。
