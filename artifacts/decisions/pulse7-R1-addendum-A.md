# R-1 追加指令（A）— 清单已确认，先补三件事  > 针对 `artifacts/worktree-inventory.md`（2026-09-10）。**清单本身通过**，工作树收口方案照 §0 执行。 > 本追加指令**插在 R-1 §1（R2）之前**：A-1 与 A-2 完成并回报后，再开始 R2 与 §2 三问。 > 全程仍在唯一开发树 `E:\codex-worktrees\win7-agent\pre-phase3` 上进行。  ---  ## 清单确认结论  以下几条据清单认定，不必重查：  - 开发树干净，`rc-0.11` 解引用 = `ed3731a` = `pre-phase3` HEAD； - F1–F4 四个 commit 线性叠在 `3303809` 上，无分叉合并； - 正式 F1–F4 证据无一产自 `E:\win7-agent`； - 归档副本（B / C）与 git 的差异**全部是 CRLF/LF**，无其他字节差异——内容对得上。  **你主动交代的三处溯源限制是正确做法**，尤其是拒绝声称"从干净 `f9634e6` 构建"。下面就是补这个缺口。  ---  ## A-1 —— 补上"被测源码 = 已提交源码"的证明（**最高优先级**）  ### 问题  最终 130 项全通过的 `final2-amd64.test.exe` / `final2-386.test.exe`，以及上传 Win7 的 `pulse7-amd64.exe`，均在 F4 提交前用**未提交工作区**编译，二进制无 `vcs.revision`。`static-checks.json` 同。  \*\*因此 `rc-0.11` 这份已提交源码，至今没有被完整跑过一次。\*\* 通过的是一份未留快照的中间状态。它与 `f9634e6` 大概率一致，但"大概率"不是证据。  ### 要做  \*\*A-1.1 复现构建比对\*\*  在干净的 `ed3731a`（工作树须 `git status --porcelain` 为空）上，\*\*用与上轮相同的目录、相同的命令、相同工具链\*\*（`E:/win7-agent/tools/go/bin/go.exe`，`go1.20.14`，`CGO_ENABLED=0`，`GOOS=windows`）重新编译 `pulse7-amd64.exe`，与已记录的 `c87065a6b02c89ae1b421cc65eb5b7622b5fd7cd62c30526f407b0d3962296fd` 比对 SHA256。  - 报告中\*\*写出实际使用的完整构建命令\*\*； - \*\*相同\*\* → 链条闭合，注明"已提交源码可复现出上轮受测二进制"； - \*\*不同\*\* → 不要推测原因，如实记录两个哈希，直接做 A-1.2。差异可能来自构建路径或标志，也可能来自源码，\*\*不得二选一地下结论\*\*。  \*\*A-1.2 从干净提交重跑全量（无论 A-1.1 结果如何都要做）\*\*  - 在干净 `ed3731a` 上 `go test -c` 两个架构，命名区分于上轮（如 `rc011-amd64.test.exe`）； - 上传 Win7 执行全量，两个架构各一次； - 判据：\*\*130 项，exit 0\*\*。与上轮同样的 lifecycle 波动若再次出现，如实记录、不重跑掩盖； - 新二进制 SHA256 记入 `build-hashes.json`。  \*\*这一步之后，"全量通过"才从"未验证"变成"实测通过"。\*\* 上轮的 `final2-*` 结果保留，但在报告中标注其构建源码快照未验证。  ---  ## A-2 —— 确认有没有搁浅的改动（\*\*这是本次盘点的直接目的\*\*）  `pulse7-p1-remediation-api-prep` 树（`d71c0dd`）有 6 个\*\*非 vendor\*\* 文件处于 modified：  `` ` `` agent/ansi.go agent/runner.go agent/sandbox.go agent/sbx.go agent/fileenc\_test.go agent/readpage\_test.go `` ` ``  vendor 目录那一大片几乎肯定是换行噪音（与 §4.3 中 67/67、93/93 全为 CRLF 差异同源），但\*\*这 6 个必须单独确认\*\*。  ### 要做  在该树上执行\*\*只读\*\*命令：  `` ` `` git diff --stat git diff --ignore-cr-at-eol --stat git diff --ignore-cr-at-eol -- agent/ansi.go agent/runner.go agent/sandbox.go agent/sbx.go agent/fileenc\_test.go agent/readpage\_test.go `` ` ``  - \*\*全部归零\*\* → 纯 CRLF 噪音，记一句结论即可； - \*\*仍有真实差异\*\* → \*\*这是从未进入任何 commit 的搁浅改动\*\*。把完整 diff 贴出来，\*\*不要提交、不要合并、不要清理\*\*，等裁决。同时说明：这些改动是否与 F1–F4 涉及的文件重叠（`ansi.go` / `runner.go` / `sandbox.go` / `sbx.go` 都不在 F1–F4 的改动面内，需确认）。  \*\*该树本轮仍为只读\*\*，A-2 只做诊断，不做任何写操作。  ---  ## A-3 —— 记账，不修  1. \*\*CRLF 噪音\*\*：仓库存在全局换行不一致，导致任何跨树字节比对都会出现假阳性。\*\*本轮不修\*\*（会污染 F1–F4 的 diff），记入 `f1f4-findings.md`。\*\*从现在起所有跨树比对一律加 `--ignore-cr-at-eol`。\*\* 2. `E:\win7-agent` 工作区存在未提交删除 \` D dist/rc-0.7/pulse7-rc0.7.7z``。只读树不该有删除记录。**不要恢复、不要提交**，记入 findings，由用户处理。 3. TEMP 下的旧归档、候选 exe、独立 git 夹具：**维持现状，一律不删**。R-1 之后的基线树按 git worktree add --detach ... baseline-3303809 建立、用完 git worktree remove，不复用 TEMP 归档。 4. 上轮"Win7-only 指令前的本地 F1 失败"无归档日志，保持未验证——处理正确，无需补做。  ---  ## 回报要求  A-1、A-2 完成后**单独回报一次**（追加到 `artifacts/worktree-inventory.md`` 末尾，不新开文件），内容只需：  - A-1.1 实际构建命令 + 两个哈希 + 是否相同； - A-1.2 两个架构的全量结果（项数、exit code、失败项如有）； - A-2 三条命令的输出与结论。  \*\*确认后再开始 R-1 §1（R2 三次运行）与 §2（三个问题）。\*\* 在此之前不要改动任何源码。 

