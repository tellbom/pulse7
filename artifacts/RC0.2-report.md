# RC 0.2 报告（Pre-RC0.2-Task 执行）

- 分支：`master`（合并 `pre-rc02` → `prompt-tune-s3` → `m4-usability` 全链）；tag `rc-0.2`（52a3bc2）
- 约束核查：go.mod/go.sum 零 diff ✅；M3 安装器产物零触碰 ✅；三旧 tag 未动 ✅；凭据未动 ✅；VM 仅软重启（2 次） ✅

## T1–T6 逐项

| 任务 | 结果 |
|---|---|
| T1 提示词回归 | ✅ S1 6 轮直接动手 / S2 13 轮直接动手 / S3 2 轮只读+提问 → **无过纠正** |
| T2 固化回归集 | ✅ reset.cmd + run-all.cmd.template + metrics.py + README |
| T3 stdout 丢失 | ✅ tee 到 data\logs\agent.log；真机 S5 实证 agent.log 有完整输出 |
| T4 压缩 audit | ✅ `_compress` 审计记录；S5 真机 5 次压缩全部有记录（含前后消息数与 token 估算） |
| T5 退出码 | ✅ 0=完成 / 1=错误 / 2=以提问结束 / 130=中断；README 已说明 |
| T6 RC 0.2 | ✅ Gate A Sandboxie PASS / Gate B JobObject PASS；包 33MB `70f8137b...` |

## 三场景对照表

| 场景 | 轮次 | 工具调用 | 写操作 | 是否提问 | 是否收敛 |
|---|---|---|---|---|---|
| S1 修 bug | 6 | 7 | 1 | 否 | ✅ |
| S2 加功能 | 13 | 21 | 8 | 否 | ✅ |
| S3 模糊任务 | 2 | 3 | 0 | **是** | ✅（以问题收尾） |

## T7 真实项目压测（E:\process）

| 指标 | R1 明确修改 | R2 探索 |
|---|---|---|
| 轮次 | 10 | 30（撞上限） |
| 工具调用 | 10（read 3 + grep 2 + edit 4 + ckpt 1） | 143（**ls 105** + read 26 + glob 10 + shell 2） |
| 耗时 | 103 秒 | ~340 秒 |
| 压缩 | 0 次 | **22 次** |
| 工作区改动 | **仅 1 文件 4 行** ✅ | **零** ✅ |
| 收敛 | ✅ | ❌（空终答） |

**核心发现**：R2 的失败不在检索速度（grep 不是瓶颈），在探索策略——模型不知道何时停止遍历目录。详见 `artifacts/T7-real-project-report.md`。

ripgrep 13.0.0 在 Win7 真机可用（~50ms vs 自写 ~300ms），当前规模**够用**。

## T8 差距分析

30 项能力评估完成，三选一判断无一"视情况而定"。**建议做 2 项**（探索策略提示词优化 + 项目记忆扩展），**暂不做 10 项**，**不做 18 项**。详见 `artifacts/T8-gap-analysis.md`。

## 最终三件事

1. 探索策略提示词优化（零代码，直接解决 R2 失败）
2. 项目记忆——AGENT.md 扩展为自动积累改动摘要（~30 行）
3. 真实内网用户试用（2-3 个 IT 同志，3-5 个真实任务）
