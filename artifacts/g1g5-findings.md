# G1–G5 Findings

## 最终状态（2026-09-11，覆盖下方历史阶段状态）

G1–G5最终验收证据见g1g5-report.md。G4规格冲突已由v2解除，G1首版的管理器重建重复已补正；这些不再是当前阻断。

本节点未修/未验证项：

1. 最近保底组在48000默认预算下也可能超过预算，实际触发阈值未知。历史窄配置需要4182字节、可用3424字节；不能推断默认预算永不失败。已同步api-contract.md。
2. hard link对checkpoint/rollback的影响未专项实测；本节点不枚举硬链接名称、不改变多链接能力拒绝。
3. “本次任务改动了哪些文件”是阶段三结束摘要契约待办，不等于普通status计数，已同步api-contract.md，本节点未实现。
4. `.git`的read/ls/tree/grep/glob拒绝残留、全局CRLF噪音、历史树未提交删除 `E:\win7-agent\dist\rc-0.7\pulse7-rc0.7.7z` 保持原状。
5. LUID保证本机重启前唯一，满足进程内要求；跨重启历史任务ID唯一性不在本节点保证范围。
6. S3精确问题数仍为判据问题；默认CLI与rc-0.7的30/100轮次差异仍在；R2窄配置历史不收敛，本节点按任务书不重跑；真实模型证据来自aigc789.top，内网速度/行为未测。
7. S1在无用户Git仓库模式下首次checkpoint计数1，日志标private-checkpoint-repo；没有额外验证该模式HEAD标签语义或扩展任务归属规则。外来nongit-checkpoint两份文档未修改内容、未纳入提交；后续专项不混入本节点。

两个lifecycle用例最终专项均20/20，最终双架构全量各138项、0失败/跳过；本次未观察到残留波动，不外推为长期失败率为零。

失败披露：G1原时间戳红灯、首版重建红灯均保留；G2来源文案红灯保留；G3首探针因管道继承等待未抓现场，修探针后补证；G4首夹具不是真实干净克隆、cmd引号错误、中文路径未用-z枚举等采集错误均保留。后置checkpoint采集器首次按session文件名猜3303809的ref查错；依据已保存工具结果只读解析真实ref后验证tree一致，未重跑checkpoint挑结果。对应原日志与修正日志均在g1g5-evidence。

---

## 历史记录（以下为当时状态，最终结论以上节为准）

## 当前阻断：G4定义待裁决

普通索引status计数描述采集时相对仓库基准的变化，包含任务前已有改动；它不直接表达本次任务归属。现有首次AUTO checkpoint在写入前执行，历史R1此时普通status为空；保持该时点应为0，写后才为1。详见g1g5-report.md。未自行增加任务基线、改变checkpoint时点或增加后置摘要。

## 任务书要求留账，后续同步api-contract.md

以下为历史结果/任务书记账要求，本节点尚未新增验证，也尚未修改api-contract.md：

- 最近保底组超过预算在48000默认预算下也可能发生，触发阈值未知；历史窄配置最近组4182字节、可用3424字节，本轮不重跑R2。
- hard link对checkpoint/rollback的影响未专项实测。
- lifecycle修复效果未验证：G1与G3尚未实施。历史失败率不能当作修复结果。
- `.git` read/ls/tree/grep/glob拒绝残留、CRLF全局噪音、只读历史树的未提交删除保持原状。

## v2裁决后的状态更新

G4定义冲突已由273af97归档的v2解除；旧暂停段是历史记录。任务改动归属清单已列入api-contract.md的阶段三待办，本节点不实现。
G1/G3专项各20次均通过；全量尚待执行。G3首个外部探针因管道继承等待错误未抓到存活PID，保留失败记录；v2探针实测ping存活约3.085秒。G4首夹具手工改CRLF后普通status自身报2（diff内容为空），没有符合干净克隆前提；改为真实干净克隆，保留0断言并通过。该Git现象不修产品、不做换行归一化。
G4初次回归启动命令的cmd引号错误导致exit255，未当作测试结果；改用Python argv启动，失败和后续日志均保留。

G1首版的管理器重建ID重复已在本节点补正，不能只报告首版50次通过；相关红灯日志保留。最终原生LUID只保证本机重启前唯一，不延伸为跨重启历史任务身份保证。
业务夹具第一次准备用未加-z的ls-files读取中文路径，被Git引号转义后当成文件名而失败；改为-z枚举并重新核对两棵业务克隆，旧失败保留。没有修改业务源码。
